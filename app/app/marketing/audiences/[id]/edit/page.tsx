import { notFound } from "next/navigation";
import { Content, PageHeader } from "@/components/shell";
import { MarketingAudienceForm } from "@/components/marketing-audience-form";
import { currentUser } from "@/lib/current-user";
import { audienceBuilderOptions, canManageAudience, validateAudienceConfig } from "@/lib/marketing-audiences";
import { prisma } from "@/lib/prisma";
export const dynamic="force-dynamic";
export default async function EditAudiencePage({params}:{params:Promise<{id:string}>}){const id=Number((await params).id),actor=await currentUser();if(!Number.isSafeInteger(id)||id<=0)notFound();const [audience,options]=await Promise.all([prisma.marketingAudience.findUnique({where:{id}}),audienceBuilderOptions(prisma)]);if(!audience||!canManageAudience(actor,audience))notFound();if(audience.archivedAt)return <Content><PageHeader title={audience.name}/><div className="panel p-6">Restore this audience before editing it.</div></Content>;let config;try{config=validateAudienceConfig(audience.filterConfig)}catch(error){return <Content><PageHeader title={`Edit ${audience.name}`}/><div role="alert" className="panel border-red-200 p-6 text-red-800">{error instanceof Error?error.message:"Saved filters are invalid."}</div></Content>}return <Content><PageHeader eyebrow="Marketing Audiences" title={`Edit ${audience.name}`} description="Build a reusable Contact audience using current SalesHub data."/><MarketingAudienceForm audience={{id:audience.id,name:audience.name,description:audience.description,visibility:audience.visibility,filterConfig:config}} options={options}/></Content>}
