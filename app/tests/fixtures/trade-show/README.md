# Trade Show workbook fixtures

These `.xlsx` files contain synthetic people, companies, domains, badge IDs, and survey answers. They model only the two known Trade Show export profiles and contain no production data.

Regenerate them from the adjacent source definitions with:

```sh
node tests/fixtures/trade-show/generate.mjs
```
