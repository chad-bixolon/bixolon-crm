'use client';
import Link from 'next/link';
import { usePathname, useSearchParams } from 'next/navigation';
import { formatCurrency } from '@/lib/display-format';
import type { PipelineReportResult, ReportConfiguration } from '@/lib/reporting';

const metricLabel:Record<string,string>={pipeline:'Pipeline',weightedPipeline:'Weighted Pipeline',opportunityCount:'Opportunities',averageOpportunityValue:'Average Opportunity Value'};
export function ReportResults({result,config,groupKey}:{result:PipelineReportResult;config:ReportConfiguration;groupKey?:string}){
 const pathname=usePathname(),searchParams=useSearchParams(); const groupHref=(key?:string)=>{const query=new URLSearchParams(searchParams.toString());if(key)query.set('group',key);else query.delete('group');return `${pathname}?${query}`;};
 const detail=groupKey?result.rows.filter(row=>row.groupKeys.includes(groupKey)):result.rows;
 return <div className="mt-6 space-y-5"><p className="text-sm text-slate-600">{result.semanticNote}</p>
  <section><div className="mb-3 flex flex-wrap items-baseline justify-between gap-2"><h2 className="text-lg font-semibold">Summary</h2>{result.currencies.length>1&&<p className="text-xs text-slate-600">Totals are shown separately by currency.</p>}</div><div className="grid gap-3 sm:grid-cols-2 lg:grid-cols-4">{result.summary.flatMap(summary=>config.metrics.map(metric=><div className="panel p-4" key={`${summary.currency}-${metric}`}><p className="text-xs uppercase tracking-wide text-slate-500">{metricLabel[metric]}{metric==='opportunityCount'?'':` · ${summary.currency}`}</p><p className="mt-2 text-2xl font-semibold">{metric==='opportunityCount'?summary.opportunityCount:formatCurrency(Number(summary[metric as 'pipeline'|'weightedPipeline'|'averageOpportunityValue']),summary.currency,result.currencies.length>1)}</p></div>))}</div></section>
  {!!result.groups.length&&<section className="panel overflow-x-auto"><h2 className="p-5 text-lg font-semibold">Grouped results</h2><table className="w-full min-w-[700px] text-sm"><thead className="bg-slate-50"><tr><th className="p-3 text-left">Group</th><th className="p-3 text-left">Currency</th>{config.metrics.map(metric=><th className="p-3 text-right" key={metric}>{metricLabel[metric]}</th>)}</tr></thead><tbody className="divide-y">{result.groups.flatMap(group=>group.metrics.map((summary,index)=><tr key={`${group.key}-${summary.currency}`}><td className="p-3">{index===0?<Link className="text-orange-800 underline" href={groupHref(group.key)}>{group.label}</Link>:''}</td><td className="p-3">{summary.currency}</td>{config.metrics.map(metric=><td className="p-3 text-right" key={metric}>{metric==='opportunityCount'?summary.opportunityCount:formatCurrency(Number(summary[metric as 'pipeline'|'weightedPipeline'|'averageOpportunityValue']),summary.currency,true)}</td>)}</tr>))}</tbody></table></section>}
  <section className="panel overflow-x-auto"><div className="flex items-center justify-between p-5"><h2 className="text-lg font-semibold">{groupKey?'Drill-down':'Details'} ({detail.length})</h2>{groupKey&&<Link className="btn-secondary" href={groupHref()}>Clear drill-down</Link>}</div><table className="w-full min-w-[900px] text-sm"><thead className="bg-slate-50"><tr>{config.columns.map(column=><th className={`p-3 ${['value','weightedValue'].includes(column)?'text-right':'text-left'}`} key={column}>{({opportunity:'Opportunity',account:'Account',owner:'Owner',stage:'Stage',closeDate:'Close Date',value:'Value',weightedValue:'Weighted Value',currency:'Currency'} as Record<string,string>)[column]}</th>)}</tr></thead><tbody className="divide-y">{detail.map(row=><tr key={row.id}>{config.columns.map(column=><td className={`p-3 ${['value','weightedValue'].includes(column)?'text-right':'text-left'}`} key={column}>{column==='opportunity'?<Link className="text-orange-800 underline" href={`/opportunities/${row.id}`}>{row.opportunity}</Link>:column==='account'?row.accounts:column==='owner'?row.owner:column==='stage'?row.stage:column==='closeDate'?row.closeDate??'—':column==='value'?formatCurrency(Number(row.value),row.currency):column==='weightedValue'?`${formatCurrency(Number(row.weightedValue),row.currency)} (${row.probability}%)`:row.currency}</td>)}</tr>)}</tbody></table>{!detail.length&&<p className="p-5 text-sm text-slate-500">No Opportunities match this report.</p>}</section>
 </div>;
}
